# Quickstart: Landing Page

To view the landing page mockup:

1. Ensure dependencies are installed:
   ```bash
   flutter pub get
   ```
2. Run the application:
   ```bash
   flutter run
   ```
3. The app will launch directly into the Landing Page.
